@model IEnumerable<MasterChef.Web.Models.IngredientModel>
<br />
<br />
<div class="container p-4 border">
    <div class="row pb-2">
        <div class="col">
            <h1 class="text-primary">Ingredientes</h1>
        </div>
        <div class="col text-end pt-1">
            <a asp-action="IngredientCreate" class="btn btn-outline-primary">
                <i class="fas fa-plus"></i>  Criar Novo Ingrediente
            </a>
        </div>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th>
                    Ingrediente
                </th>                           
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.Name
                    </td>                                     
                    <td>
                        <a asp-action="IngredientUpdate" class="btn btn-success" asp-route-id="@item.Id">
                            <i class="fas fa-edit"></i>
                        </a>
                        <a asp-action="IngredientDelete" class="btn btn-danger" asp-route-id="@item.Id">
                            <i class="fas fa-trash"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>